# 后端配置
PORT=3000
NODE_ENV=development

# 数据库配置 (阿里云本地 Docker 内部连接)
DB_HOST=db
DB_PORT=3306
DB_USER=root
DB_PASSWORD=your_secure_password
DB_NAME=nocturne_sync

# 安全配置
JWT_SECRET=nocturne_reader_secret_key

# 前端集成配置
# VITE_API_URL=https://your-cloud-domain.com
